{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main dashboard and the reminder popup",
  "windows": ["main", "popup"],
  "permissions": [
    "core:default",
    "core:window:allow-is-visible",
    "core:window:allow-is-focused"
  ]
}
